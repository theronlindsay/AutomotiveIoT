# Raspberry Pi Dashcam Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.28.0
pyserial>=3.5

# For Pi Camera (Raspberry Pi OS Bullseye+)
# Note: picamera2 should be installed via apt on Raspberry Pi:
#   sudo apt install python3-picamera2
# Uncomment below if installing via pip:
# picamera2>=0.3.12

# For USB cameras or video processing
opencv-python>=4.8.0

# Optional: For better performance
numpy>=1.24.0
